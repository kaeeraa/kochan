from os.path import dirname, join

static_path = join(dirname(__file__), "..", "static")
images_path = join(static_path, "images")
css_path = join(static_path, "css")
fonts_path = join(static_path, "fonts")
